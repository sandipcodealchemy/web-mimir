import React, { FC } from 'react'

export const Loading = ({ className }: { className?: string }) => {
  return (
    <div className={className}>
      <h2 className="text-Ellipse0001">🌀 Loading...</h2>
    </div>
  )
}
